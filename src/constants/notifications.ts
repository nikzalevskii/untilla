import type { NotificationOffset } from '@/types'

// Offsets applied when user enables notifications on a countdown
export const DEFAULT_NOTIFICATION_OFFSETS: NotificationOffset[] = ['1d', 'day_of']
